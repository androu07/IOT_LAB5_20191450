<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color del fondo, si lo deseas -->
    <solid android:color="#FFFFFF" />

    <!-- Definición del borde -->
    <stroke
        android:width="2dp"
        android:color="#000000" /> <!-- Color negro para el borde -->

    <!-- Esquinas redondeadas (opcional) -->
    <corners android:radius="8dp" />

    <!-- Margen interno (padding) -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" />
</shape>